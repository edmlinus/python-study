#contents to be added
# annotations are marked with # in python.

list1 = [1,2,3]

#write something.
a = 1
b,c = 2,3
print(a)
b = c+a

#write more lines of code.. make editions. careful not to screw up the performance of other parts of the code,
#saving changes here will only change the state of codes stored in your local repository, not the origin.
#you must first commit your changes, and then push it to origin, to change the codes at the origin.